package io.github.railgun19457.dummy.common.config;

public record TraitDefaultConfig(
        boolean collidable,
        boolean invulnerable,
        boolean autoRestock,
        boolean allowInventoryOpen,
        boolean allowInteractionConfigure
) {
}
